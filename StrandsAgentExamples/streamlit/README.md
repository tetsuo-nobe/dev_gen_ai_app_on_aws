# Strands Agents + Streamlit

```
pip3 install streamlit
```

* config.toml は、プロジェクトフォルダまたはユーザーのホームディレクトリの .streamlit フォルダに格納
    - 設定が読み込まれているか確認

      ```
      streamlit config show
      ```
      
